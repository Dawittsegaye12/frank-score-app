# Vercel-specific requirements
# Note: Some packages may need to be adjusted for Vercel's serverless environment

# Core FastAPI dependencies
fastapi==0.115.6
mangum>=0.17.0  # Required for Vercel ASGI adapter
pydantic==2.9.2
python-multipart==0.0.9

# Template engine
jinja2==3.1.4

# Database (SQLite - note: ephemeral on Vercel)
# SQLite is built into Python, no package needed

# ML Libraries (may be large - consider alternatives)
# Note: XGBoost and scikit-learn are large packages
# Vercel has size limits, consider using external API for ML predictions
joblib>=1.3.0
# xgboost>=2.0.0  # Comment out if too large, use external API instead
# scikit-learn>=1.0.0  # Comment out if too large
numpy>=1.24.0
pandas>=1.5.0

# Explainability (optional, can be large)
# shap>=0.41.0  # Comment out if too large
# lime>=0.2.0.1  # Comment out if too large

# HTTP client
requests>=2.31.0

# Note: For production, consider:
# 1. Using external API (Render) for ML predictions
# 2. Using external database (PostgreSQL, MongoDB)
# 3. Serving static files from Vercel, API from Render

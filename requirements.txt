# Vercel-compatible requirements
# Note: Large ML packages (XGBoost, scikit-learn) are excluded for Vercel
# Use external API (Render) for ML predictions

# Core FastAPI dependencies
fastapi==0.115.6
mangum>=0.17.0
pydantic==2.9.2
python-multipart==0.0.9

# Template engine
jinja2==3.1.4

# Database (SQLite is built into Python)
# No package needed

# Basic data processing (lightweight)
joblib>=1.3.0
numpy>=1.24.0
pandas>=1.5.0

# HTTP client
requests>=2.31.0

# Note: XGBoost, scikit-learn, shap, lime are excluded
# These are too large for Vercel's 50MB limit
# Use Render deployment for ML predictions: https://frank-score-app.onrender.com
